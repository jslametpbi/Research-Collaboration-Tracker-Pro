JS Research Collaboration Command Center - GitHub Pages Ready

This package uses a single self-contained index.html file with embedded CSS and JavaScript.
Upload these files to the root of your GitHub repository:

index.html
favicon.svg
.nojekyll
README_DEPLOY.txt

Then activate GitHub Pages: Settings > Pages > Deploy from a branch > main > /root > Save.
